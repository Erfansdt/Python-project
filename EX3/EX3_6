def EX3_6(int_list):
    '''
    ([int]) - > str
    '''
    interval=max(int_list) - min(int_list)
    face = ''
    for number in int_list:
        if number == interval:
            return ':)'
        else:
            return ':('
    return face
